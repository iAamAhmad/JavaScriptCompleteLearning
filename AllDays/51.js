//? iterables
/*
    iterables are those objects which can be iterated over
    iterables are objects which have a method called Symbol.iterator
    which returns an iterator object
    iterator object has a next method which returns an object
    with two properties
    value - the value of the current iteration
    done - a boolean value which is true if the iteration is over
    and false if the iteration is not over
    iterables are objects which have a method called Symbol.iterator
    which returns an iterator object
    iterator object has a next method which returns an object
    with two properties

*/
//? array like objects
/*
    array like objects are objects which have a length property
    and the properties are indexed from 0 to length - 1
    array like objects are not iterables
    array like objects are not iterables
    array like objects are not iterables
*/